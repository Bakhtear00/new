import React from 'react';

const ConfigurationNotice: React.FC = () => {
    // This component is no longer needed as the app doesn't require cloud configuration anymore.
    // Returning null removes it from the UI.
    return null;
};

export default ConfigurationNotice;